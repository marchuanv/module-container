const utils = require("utils");

module.exports = {
    handle: async ({ session, requestContent }) => {
        const { writeFileSync, mkdirSync, existsSync, readFileSync, rmSync } = session;

    }
}